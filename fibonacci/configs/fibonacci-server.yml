# GRPC server settings
grpc:
    host: fibonacci
    port: 55555

# HTTP server setting
http:
    host: fibonacci
    port: 8080

# Redis database settings
db:
    host: redis
    port: 6379
    password:
    name: 0

