syntax = "proto3";
package fib;

option go_package = "./;fibpb";

service Fib {
	rpc Get (Request) returns (Response) {}
}

message Request {
	uint64 from = 1;
	uint64 to = 2;
}

message Response {
	repeated string numbers = 1;
}
